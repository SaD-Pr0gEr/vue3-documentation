// v-show используется для видимости объектов и их скрытия
// т.е. у них будет display:none
// $emit привязывает(вызывает) метод с другого компонента(для этого надо её вызывать в $emit() и на шаблоне прописать
// @my_emit='call-function') и в emit надо передать название в js файле: this.$emit('my_emit', params: optional)
